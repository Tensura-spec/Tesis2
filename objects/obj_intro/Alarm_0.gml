/// @description  Display intro message

// Create the message
with (instance_create(0, 0, obj_message)) {
    
    // Exit automatically
    switchmsg = 2;
    
    // Grab the text
    inisection = other.inisection;
    
}

