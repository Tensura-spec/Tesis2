/// @description Bump with block but only when not held

//If not held
if (held == false) {
    
    if (other.vspeed < 0)
    && (vspeed >= 0)
    && (bbox_bottom < other.yprevious+5)
        vspeed = -3;
}

